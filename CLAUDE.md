# Starshopping — төслийн гарын авлага

Энэ файлыг **шинэ сесс эхлэх бүрд эхлээд уншина**.

> **Аль бичиг юуны эх сурвалж вэ** (backend чаттай 2026-09-20-нд тохирсон):
> API-ийн гэрээ = эзний `WEBSITE-INTEGRATION.md`; чат хоорондын даалгавар =
> эзний `_bus/` хавтас (`to-web/` уншина, `to-backend/` бичнэ) — **сесс эхлэхэд
> `_bus/to-web/`-ийг заавал шалга**; энэ `CLAUDE.md` = вэб талын дотоод бичиг.
> Эдгээр нь энэ repo-д биш, эзний Mac дээрх `Claude Starshopping drop` хавтсанд. Хуучин ярианы түүхийг
дахин ярих шаардлагагүй — шаардлагатай бүх зүйл энд бий.

---

## 1. Энэ юу вэ

Монгол dropshipping дэлгүүр. Reels/TikTok-ийн зараас ирсэн хүн бараа сонгож,
захиалга өгөхөд эзний **n8n intake → Supabase** руу бүртгэгддэг (2026-09-17-оос;
өмнө нь Google Sheet). **Бараа** (нэр, үнэ, зураг, нөөц) 2026-09-18-аас мөн
Supabase-аас уншигдана — **шинэ бараа = n8n маягт 13 бөглөх** (§17). Sheet
дэлгүүрийн мэдээлэл, категори, багц, сэтгэгдлийг л өгнө.

| | |
|---|---|
| **Амьд сайт** | https://starshopping.mn (2026-09-20-оос; хуучин `starshopping-mn.github.io` 301-ээр энд шилждэг — §18) |
| **Repo (үндсэн)** | `starshopping-mn/starshopping-mn.github.io` — remote нэр `origin` |
| **Хуучин repo** | `ariunboldagency-commits/Starshopping` — remote нэр `oldsite`, зөвхөн redirect |
| **Локал зам** | `C:\Users\ariun\Documents\Starshopping-web` (Windows). Mac дээр тогтмол хуулбар байхгүй — `git clone` хийж ажилла; `gh`, `node` суугаагүй |
| **Sheet** | [Starshopping — Каталог ба Захиалга](https://docs.google.com/spreadsheets/d/1ZU7rJfHKFqoK7taHpVYQAC3GnFbPGyo83vTgWlVqakM/edit) |
| **Эзэн** | ariunbold.agency@gmail.com |
| **Meta Pixel** | 1409055724744932 (эзэн 2026-08-07-нд GitHub дээрээс шууд сольсон — өмнөх нь `1673931670377637`, буруу Business portfolio-д харьяалагдаж байсан) |

Одоо байгаа бараа/зураг бол **тестийн placeholder**. Бодит бараа хараахан алга.

---

## 2. Архитектур

```
Хөтөч (GitHub Pages, статик)
  index.html · style.css · script.js · vendor/gsap · assets/
        │
        ├─ POST → бараа (нэр, үнэ, хямдралын өмнөх үнэ, зураг, нөөц, product_id)
        │         Supabase RPC `web_products` — anon key, зөвхөн унших
        │         ← n8n маягт «13 · Бараа бүртгэх» → `upsert_product`
        │
        ├─ GET  → дэлгүүрийн мэдээлэл, хүргэлт, категори, багц, сэтгэгдэл
        │         (+ ижил slug-тай мөрөөс өнгө, хэмжээ, хугацаа — түр, §17)
        │         Google Apps Script Web App (apps-script/Code.gs) → Google Sheet
        │
        └─ POST → захиалга
                  https://starshopping.app.n8n.cloud/webhook/order-intake
                  → n8n → Supabase `intake_order()` — үнэ, нөөц, давхардлыг
                    СЕРВЕР шийднэ. Сайт юу ч тооцохгүй, үнэ илгээхгүй.
```

**Каталогийн ачаалалт** (`script.js` төгсгөл, `fromSupabase()`): гурван эх
сурвалжийг **зэрэг** асууна, нэг нь нөгөөгөө хүлээхгүй — reel-ийн deep link
3G дээр удаашрах ёсгүй. Supabase-ийн мөрүүд ирмэгц Sheet-ийн (эсвэл
`catalog.json`-ы) `shop, categories, bundles, reviews` дээр **бараагаа сольж**
тавина. Supabase **хоосон** буцаавал алдаа биш — Sheet-ийн бараа өмнөх шигээ
явна. `tools/catalog.py` яг ижил дүрмээр уншдаг тул карт, эрүүл мэндийн
шалгалт, хуудас гурав нэг үнэ харуулна.

**Захиалгын гэрээ** (эзний `WEBSITE-INTEGRATION.md`-ээс): body нь
`{ product_id, name, phone (8 орон), address (нэг мөр), quantity,
channel: "web", creative_id }`. Хариу `ok:true` → `order_id, product,
total_mnt, is_duplicate…`; `ok:false` → `error` / `errors[]` + `message`.
`script.js` → `ORDER_INTAKE`, `orderRefusal()`.

**Каталог → Supabase (2026-09-18).** `SUPABASE_URL`, `SUPABASE_ANON`,
`CATALOG_SOURCE` — `script.js` толгойд. Anon key **публик** (зочин бүрийн
хөтчид байдаг), зөвхөн `web_products`-ыг уншина; `service_role`-ыг ХЭЗЭЭ Ч
фронтэнд, `tools/`, GitHub дээр тавихгүй; `orders/customers/ad_spend/
cost_settings`-ийг сайт хэзээ ч уншихгүй. Мөрийн талбарууд:
`product_id, slug, name, description, images|image_urls, price_mnt,
compare_at_mnt, stock_qty|in_stock, category, featured, status`. Хөрвүүлэлт:
`price ← price_mnt`, `compareAt ← compare_at_mnt` (их бол зурсан хуучин үнэ,
`priceOf()` хувь бодохгүй), `desc ← description`, `stock[slug] ← stock_qty`
(байхгүй бол `in_stock:false` → 0, чимээгүй бол хязгааргүй — Sheet-ийн хуучин
тоо **арилна**). Slug-гүй, үнэгүй мөр алгасна. Захиалга **`product_id` =
Supabase UUID** явуулна (`p.product_id`); n8n intake UUID биш ирвэл
`product_not_found` буцаана.

**Attribution.** Зарын холбоос `…/p/<slug>?ref=<creative_id>`. Карт хуудас
`location.search`-ийг дэлгүүр рүү дамжуулна, `script.js` эхний ачаалалт дээр
`localStorage.ss_ref`-д хадгална, захиалгад `creative_id` болж явна.

Framework байхгүй, локал build алхам байхгүй. Гурван файл + GSAP.

**Ганц үл хамаарах зүйл — холбоосын карт.** `tools/build-og.py` нь GitHub
дээр (локал биш) ажиллаж, Sheet-ээс бараа бүрд `p/<slug>/index.html` карт
хуудас, `og/<slug>.jpg` 1200×630 зураг үүсгэнэ. Facebook-ийн робот
JavaScript ажиллуулдаггүй, `#`-ийн ард харагддаггүй тул SPA-гийн холбоос
зураг, нэр, үнэгүй харагддаг байсан. Карт хуудас нь OG шошгыг markup дотор
барьж, хүнийг JS-ээр дэлгүүр рүү шиднэ (meta refresh **биш** — тэр нь
роботыг ч дагуулж явчихна).

Дэлгэрэнгүйг §11-ээс үз.

**Чиглүүлэлт:** hash-based (`#/c/<slug>`, `#/p/<slug>`, `#/order`, `#/done`,
`#/policy/<name>`). GitHub Pages дээр сервер тохиргоо шаардахгүй, бараа бүр
өөрийн хаягтай (Pixel-д чухал).

---

## 3. ⚠️ Эвдэж болохгүй зүйлс

Эдгээрийг олоход маш их зовсон. Өөрчлөхийн өмнө хэмжиж бат.

### Hero-гийн линз рүү орох zoom — сайтын гол хүч
- `--lens-x: 52.1%` / `--lens-y: 87.5%` — зургаас **программаар хэмжсэн**
  линзний төв. Таамаглаж өөрчилж болохгүй.
- `transform-origin` нь `.hero__cam img` дээр. `lensOffset()` нь линзийг
  hero-гийн голд аваачдаг. Эцэст нь `scale 11`, линз голоос **0px** зөрөөтэй.
- Zoom-ын урт `+=130%` (2026-09-17; өмнө нь `+=190%`). Хөшиг 0.6-аас хаалттай
  тул 190% дээр сүүлийн 40% нь — утсан дээр **650px** — хоосон цайвар дэлгэц
  байсан. 130% дээр **444px**. Scale ба хөшиг нь явцтай холбоотой, зайтай биш —
  хөшиг яг өмнөх scale дээр (явц 0.595) хаагдсан хэвээр, Safari-гийн хамгаалалт
  хөндөгдөөгүй. **120%-аас доош болгохгүй** — орох хөдөлгөөн хэт богино болно.
  Эзэн утсан дээрээ `?diag`-аар үзээд таалагдахгүй бол энэ ганц тоог буцаана.

### Pin-үүдийн дараалал
- `#hero` pin → шууд `#cats` pin. **Хоорондох завсар 0 байх ёстой.**
- `.cats { margin-top: -100svh; z-index: 2 }` — үүнийг хасвал pin суларсны
  дараа hero өөрийн бүтэн дэлгэцээр хар хөшигтэйгээ урсаж, **1 дэлгэц цул
  хар** үүснэ. Энэ алдааг нэг удаа гаргаад зассан.
- `refreshPriority: 1` (hero) / `-1` (cats) — эс тэгвэл cats-ийн trigger
  сөрөг эхлэлтэй болж эвдэрнэ.

### `--chars` fallback
`font-size: min(19vw, calc(92vw / (var(--chars, 6) * 0.72)))` — fallback
заавал байх ёстой. GSAP-ийн `clearProps: "all"` нь inline style-ыг арчихад
`--chars` алга болж, calc хүчингүй болоод үсэг 16px болж унадаг.
`destroyHomeMotion()` дотор `.hero__word`-д зөвхөн `opacity` цэвэрлэнэ.

### Гуравдагч серверийг эгзэгтэй замд оруулахгүй
Google Fonts-ыг render-блоклож байснаас Монголоос **4 секунд цагаан дэлгэц**
үүсдэг байсан. Одоо фонт, GSAP бүгд өөрийн серверээс. Шинэ CDN нэмэхгүй.
Үлдсэн гадаад холбоос: Meta Pixel (async), Apps Script (эгзэгтэй биш).

### Safari-гийн давхаргын хязгаар — hero-гийн zoom
iPhone 11 Pro дээр `?diag`-аар хэмжсэн:

| Scale | Үр дүн |
|---|---|
| 2.35 | ✅ бүтэн зурагдана |
| 4.35 | ❌ дунд нь тасарна |
| 6.05 | ❌ тасарна |

`scale 11` × 3× нягтрал = хөтчөөс шаардагдах гадаргуу хэт том. **Ямар ч CSS
засвар үүнийг тойрохгүй** — давхарга физикийн хувьд багтахгүй.

Тиймээс `.hero__veil` нь **0.42-оос эхэлж 0.60 гэхэд бүрэн бүрхдэг** болсон
(өмнө нь 0.86-аас). Эвдэрдэг муж хэзээ ч харагдахгүй. Zoom-ын код `scale 11`
хэвээр — зөвхөн хэдийг нь харуулахыг өөрчилсөн.

Хөшгийг **хойшлуулж болохгүй**. Хойшлуулбал тасарсан кадр гарна.

### Zoom хийгддэг зурагт `filter` ТАВИХГҮЙ
`.hero__cam img` дээр `filter: drop-shadow(...)` байсныг **2026-08-16-нд
хассан**. Шалтгаан:

`filter` нь хөтчийг зургаа **хувиргасан хэмжээгээрээ дахин зурахад** хүргэдэг —
GPU-гээр хямдхан томруулахын оронд. Энэ зураг `scale 11` хүртэл томордог:
622px өндөр × 11 = 6,842px, iPhone-ы 3× нягтралаар **20,500 пиксел**. Safari
ийм том гадаргуу хуваарилдаггүй.

Утсан дээрх шинж тэмдэг: **халалт, гацалт, камерын зөвхөн дунд зурвас
зурагдаж дээр доор нь нүцгэн дэвсгэр**. Оношилгоо (`?diag`) дээр зураг
"өндөр 1109px, доод ирмэг 609" гэж мэдээлж байсан — өөрөөр хэлбэл дэлгэцийг
бүтнээр хамрах ёстой байсан атлаа зурагдаагүй.

Ижил дүрэм: zoom, scale хийгддэг ямар ч элементэд `filter`, `backdrop-filter`
тавихгүй. Хөдөлдөггүй зурагт (`.cat__img img`) асуудалгүй.

### `?diag` — утасны хэмжигч
`script.js` дотор. Хаягт `?diag` нэмэхэд дэлгэцийн өндөр, hero-гийн өндөр,
камерын доод ирмэг, **нүцгэн зай**, zoom-ын явц, scale-ыг шууд харуулна.
Үйлчлүүлэгчид хэзээ ч харагдахгүй. Эмуляц дээр гардаггүй утасны алдааг
оношлоход хоёр удаа буруу таамагласны эцэст үүнийг хийж, шалтгааныг
нэг удаагийн хэмжилтээр олсон. Утасны алдаа гарвал эхлээд үүнийг ашигла.

### `ignoreMobileResize`-ыг БҮҮ нэм
2026-08-10-нд эзний хүсэлтээр `ScrollTrigger.config({ ignoreMobileResize: true })`
нэмсэн — "toolbar хөдлөхөд эхэнд буцаадаг" гэсэн үндэслэлээр. Тэр алдаа
эмуляц дээр **хэзээ ч давтагдаагүй**, тохиргооны үр нөлөөг ч батлаж чадаагүй.

Харин 08-16-нд жинхэнэ утсан дээр эсрэг үр дагавар илэрсэн: hero-гийн zoom
дунд камерын доор **бүтэн дэлгэц хоосон зурвас** гарч ирдэг болсон.

Учир нь hero нь `svh` — toolbar харагдаж байх үеийн өндөр — дээр
суурилдаг ба ачаалах үедээ хэмжигддэг. Гүйлгэхэд toolbar нуугдаж дэлгэц
**томордог**; ScrollTrigger дахин хэмжиж чадвал pin тохирно, харин энэ
тохиргоо тэр дахин хэмжилтийг хориглодог. Үр дүнд нь pin жижиг хэмжээндээ
үлдэж, доор нь нүцгэн дэвсгэр гарна.

Устгасан. Дахин нэмэх шаардлага гарвал эхлээд **жинхэнэ утсан дээр** алдааг
давтаж бат — эмуляц энэ хоёрын алийг нь ч харуулдаггүй.

### `?ref=` — зарын мөр тасарч болохгүй
- Карт хуудасны redirect нь **`location.search`-ийг заавал дамжуулна**
  (`tools/build-og.py` → `PAGE`). Тогтмол хаяг руу шидвэл `ref` алга болж, аль
  зар захиалга авчирсныг хэзээ ч мэдэхгүй. 2026-09-17 хүртэл яг ингэж алдагдаж
  байсан.
- `ss_ref`-ийг захиалгын дараа **устгахгүй** — давтан худалдаа мөн тэр зарынх.
  Өөр зараар дахин орвол дарж бичигдэнэ.
- `localStorage` хаалттай (private цонх) байж болно — `try/catch` дотор,
  дэлгүүрийг хэзээ ч унагаахгүй.

### Барааны холбоос эхний зурагнаас бараан дээрээ нээгдэнэ
`index.html`-ийн хоёр inline скрипт (2026-09-08-ны засвар): `<head>`-д,
**stylesheet-ийн ДЭЭР**, хаягийг уншиж `<html data-route>` тавина, hero
зургийн preload-ыг зөвхөн нүүрэнд үүсгэнэ; `</main>`-ийн дараах нь home-ыг нууж
skeleton зурна. Өмнө нь кэшгүй зочин Fast 3G дээр **3.2с нүүр хуудас** хардаг
байв. Скриптийг stylesheet-ийн доор зөөвөл preload 780ms хоцорно. Hero `<img>`
`loading="lazy"` — нуугдсан home 224KB татахгүй.

### Feed ирэхэд барааны хуудсыг дэмий дахин зурахгүй
`paint()` нь `productSignature()`-ээр өмнөх/дараах өгөгдлийг харьцуулж, өөрчлөлтгүй
бол `renderProduct`-ыг алгасна. Өмнө нь амьд сайт дээр 6.4с-т галерей солигдож
1-р зураг руу үсэрдэг байв. `renderProduct`-д шинэ өгөгдлийн эх сурвалж нэмбэл
**signature-т мөн нэм**, эс бөгөөс өөрчлөлт зурагдахгүй.

### Барааны хуудасны доод самбар (`.stickybuy`)
Утсан дээр жинхэнэ товч 1183px доор байсан. Самбар нь жинхэнэ товч дэлгэцэнд
байхгүй үед л харагдана (`IntersectionObserver`), дарахад **жинхэнэ товчийг
дарна** — форм руу очих зам нэг хэвээр. `env(safe-area-inset-bottom)`-оор
доторлогоотой. `backdrop-filter` тавихгүй. Дахин зурах бүрд хуучныг устгаж,
observer-ийг салгана — эс бөгөөс хуучин барааны самбар үлдэнэ.

### `history.scrollRestoration = "manual"`
Хуудсыг дунд нь refresh хийхэд pin буруу цэгээс хэмжигдэж hero zoom-ын дунд
гацдаг байсан.

### Захиалгын форм — гацаж болохгүй, хаана буруу байгааг заана
2026-08-20-нд шалгалтаар олдсон зүйлс. Эдгээрийг арилгаж болохгүй:

- **Захиалга n8n intake руу явна, үнэ ИЛГЭЭХГҮЙ** (2026-09-17). Сайт
  урьдчилсан дүнг зөвхөн харуулна; баталгаажуулах хуудас **серверийн**
  `total_mnt`-ийг хэвлэнэ. Хүргэлтийн төлбөрийг intake мэддэггүй тул сайт
  сонгосон хүргэлтийн үнийг тусад нь мөр болгож нэмж харуулна.
- **`is_duplicate: true` бол алдаа БИШ** — хэвийн «хүлээн авлаа» хуудас.
- **Хариуг статусаар биш, body-оор шүүнэ.** Intake татгалзлаа **HTTP 400 +
  JSON**-оор өгдөг, мөн баримтад байгаа `error` мөрийн оронд `errors: [...]`
  жагсаалт буцааж байсан (хэмжсэн: `{"ok":false,"errors":["phone_invalid"]}`).
  `orderRefusal()` хоёуланг уншина, `message` байвал үргэлж түүнийг харуулна.
- **Форм: нэр, утас, хаяг (нэг мөр), тоо ширхэг.** Өмнө нь 8 заавал талбар
  (орц, тоот — гэр хороолол, суманд байхгүй) байсан. Нэр/утас нь хүргэлт,
  төлбөрийн сонголтын **дээр**.
- **Өнгө, хэмжээ, багц, хүргэлт, төлбөрийн сонголт хаягийн мөрийн төгсгөлд
  `[…]` дотор явна.** Intake-д эдгээрт талбар байхгүй; хаявал оператор
  зочны аль хэдийн хариулсан зүйлийг дахин асуухад хүрнэ. Intake талд талбар
  нэмэгдвэл тийш нь зөө.
- **POST-д 30 секундын хязгаар (`AbortController`).** Өмнө нь хязгааргүй
  байсан — сүлжээ тасрахад товч «ИЛГЭЭЖ БАЙНА…» дээр **үүрд гацаж**,
  `sending` буцаж буудаггүй тул дахин дарах ч боломжгүй байв. Мөнгө орж
  ирдэг цорын ганц дэлгэц дээрх гацалт.
- **Хугацаа хэтэрсэн үед «дахин илгээ» гэж хэлэхгүй.** Хүсэлт Sheet-д
  хүрчихээд хариу нь замдаа алдагдсан байж болно. Давхар захиалга үүсгэхээс
  утсаар залгуулах нь дээр.
- **Алдааг тухайн талбарын хажууд аваачна** (`fail()`), талбарыг улаан
  болгож, фокус тавьж, дэлгэцэнд оруулна. Өмнө нь урт формын хамгийн доор
  нэг мөр гарч, дээд талын хоосон нүдтэй хүн дарахад **юу ч болоогүй мэт**
  харагддаг байв.
- ⚠️ **Гүйлгэхдээ `scroll-behavior`-ыг түр `auto` болгоно** (`bring()`).
  Баримт нь `html { scroll-behavior: smooth }`. Smooth гүйлт бол анимаци —
  `prefers-reduced-motion` дээр алгасагдана, зарим in-app браузер дээр
  боомилогдоно. Хэмжсэн: энэ орчинд smooth гүйлт **0 пиксел** хөдөлсөн,
  instant нь 400. Алдаа заах бол анимаци биш, **заавал болох ёстой зүйл**.
- **«НЭМЭЛТ УТАС» талбар хасагдсан** (2026-09-17) — intake ганц утас авдаг.
- **Утасны нүдэнд зөвхөн цифр** — бичиж байхад шүүгдэнэ.
- **`<form>` жинхэнэ байх ёстой** — Enter товч илгээнэ, утасны гар «Дараах»
  гинж өгнө, autofill хаягийг бүлгээр нь таньна. `novalidate` — браузерын
  анхааруулга буруу хэлээр гарахаас сэргийлнэ, шалгалтыг бид өөрсдөө хийнэ.

### UUID-гүй захиалга ХЭЗЭЭ Ч явахгүй
2026-09-17 12:43-т жинхэнэ утаснаас ирсэн **хоёр захиалга алдагдсан**: `product_id`
хоосон байсан тул `d.productId || d.slug` slug илгээж, Supabase `22P02 invalid input
syntax for type uuid`-оор татгалзсан — зочин захиалсан гэж бодсон. Одоо:
- `|| d.slug` **байхгүй**. Илгээхийн өмнө `productId = d.productId ||
  productBy(d.slug).product_id`; хоосон бол **сүлжээ рүү гарахгүй**, «онлайнаар
  захиалах боломжгүй, 9550-5717» гэж хэлнэ.
- `cannotOrder(p)` = `liveLoaded && supaSettled && !p.product_id` үед «ЗАХИАЛАХ»
  товч **«УТСААР ЗАХИАЛНА»** болж хаагдана, утасны холбоос үлдэнэ. `supaSettled`
  худал байхад (Supabase хараахан хариулаагүй) юу ч хаахгүй — Sheet түрүүлж
  ирэхэд id-гүй нь «хараахан ирээгүй» гэсэн үг.
- Товчны төлөв `views.product.dataset.noid`-д хадгалагдана; `paint()` үүгээр
  дахин зурах эсэхээ мэднэ. Төлөвийг `productSignature`-т **бүү хий** — туг
  `paint()`-ийн «өмнөх» уншилтаас өмнө солигддог тул хоёр уншилт үргэлж таарч,
  юу ч дахин зурагддаггүй (хэмжиж олсон).
- n8n intake UUID биш юуг ч `product_not_found`-оор буцаана. Чимээгүй орлуулга
  аль ч талд байхгүй.

### Meta Pixel — юу, хэзээ, ямар дүнгээр (2026-09-21)
Зарын оновчлол эдгээр тоон дээр суурилдаг тул худлаа тоо = буруу зар руу мөнгө.
- **Purchase зөвхөн intake `ok:true` буцаасны дараа**, мөн `is_duplicate` **биш** үед.
  Давхардал зочинд «хүлээн авлаа» гэж харагдана, гэхдээ хоёр дахь борлуулалт биш.
- **`value` = intake-ийн `total_mnt` (барааны дүн). Хүргэлтийн төлбөр ОРОХГҮЙ** —
  тэр курьерт очдог; нэмснээр ROAS хиймлээр өсдөг байсан.
- `content_ids: [slug]`, `content_type: "product"` — ViewContent, InitiateCheckout,
  Purchase гурвууланд **ижил** (Meta бараагаар холбоно).
- Purchase-ийн 4 дэх аргумент `{ eventID: order_id }` — хожим сервер талын
  Conversions API ижил захиалгыг давхар тоолохгүйн бэлтгэл.
- **ViewContent нэг очилтод НЭГ удаа** (`viewReported`), дахин зурах бүрд биш.
  Каталог секундын дараа ирж хуудас дахин зурагддаг тул өмнө нь нэг зочин
  **хоёр ViewContent** өгдөг байв (2026-09-21, Meta Test Events дээр 2 секундын
  зайтай хоёр мөр). `route()` тэмдгийг арилгана — гараад буцаж ирвэл дахин тоолно.
- **Эхний route PageView явуулахгүй** (`routedOnce`) — `index.html` аль хэдийн
  явуулсан; өмнө нь ирэлт бүр хоёр тоологддог байв. Дараагийн route бүр явуулна.
- 2026-09-21 гинжийн тест (`?ref=TEST-CHAIN`, эзний Chrome, Test Events):
  PageView → ViewContent → InitiateCheckout → Purchase бүгд ирсэн; Purchase-ийн
  Event ID = `order_id`, `value 119000`, `content_ids ["Huuhdiin-hashiwch"]`.
- Шалгах арга: `window.fbq`-г массив руу бичдэг функцээр, `fetch`-ийг order-intake
  дээр зохиомол хариугаар сольж, ok / duplicate / refused гурвыг явуул. Бодит
  захиалга үүсгэхгүй. Гурвуулаа ингэж хэмжигдсэн.

### Хүргэлтийг хэн төлөх — захиалахаас ӨМНӨ хэлнэ (2026-09-21)
`web_products.delivery_paid_by` → `p.deliveryPaidBy` (`customer` | `included`;
талбар ирээгүй бол `customer` — одоогийн бодит байдал). `customer` үед дөрвөн
газар хэлнэ: үнийн дор `.shipline`, утасны доод самбар (`.stickybuy__ship`),
захиалгын дүнгийн мөр + тайлбар, баталгаажуулалтын мөр. `included` үед хамгийн
хямд хүргэлт 0₮ болж, үнэтэй сонголт **зөвхөн зөрүүгээ** авна (`shipBase`).
Бичлэгт үнээ аль хэдийн хэлсэн бараанд хүргэлтийг нуувал хаалган дээр татгалзана.

### Утасны дугаарууд — хэнийх, юунд (2026-09-21)
| Дугаар | Юу | Хаана |
|---|---|---|
| **9550-5717** | **Эзний дугаар = дэлгүүрийн цорын ганц шугам.** «Залгаж захиалах», «Захиалгын талаар», бүх алдааны мессеж | `script.js` → `ORDER_LINE` + мессежүүд; `index.html` → `#helpPop`; n8n WF02-ын татгалзлын мессеж |
| **9494-4855** | **Гялс хүргэлт** (gyalsdelivery.mn) — 2026-09-оос хүргэлтийн компани. «Хүргэлтийн явц, хугацаа» | `script.js` → `COURIER`; `index.html` → `#helpPop` (**хоёуланг зэрэг соль**) |

⚠️ **8810-4640 ба 9411-4495 бол ХУУЧИН, дэлгүүртэй хамааралгүй дугаарууд** — эзний
хэлснээр 2026-09-21-нд бүх газраас устгасан. Хаа нэгтээ дахин гарч ирвэл алдаа.
`shop.phones` нь **Sheet-ийн нүд БИШ** — `apps-script/Code.gs`-ийн `CONFIG.shop.phones` (23-р мөр).
Repo дахь хуулбар 9550-5717 болсон; **deploy хийгдсэн Apps Script хуучнаараа** (эзэн дараагийн
удаа Code.gs deploy хийхэд шинэчлэгдэнэ, §4). Яарах зүйлгүй: сайт тэр талбарыг **уншдаггүй**,
`tools/build-og.py` түүнийг `data/catalog.json` руу **хуулдаггүй**.

«Парцел хаана байна» бол курьерын, «юу захиалсан» бол манай асуулт — тиймээс
лавлах гурван газарт (толгойн цонх, баталгаажуулалтын хуудас, хүргэлтийн
бодлого) хоёр дугаар **тус тусын гарчигтай** байна. Курьер манай захиалгын кодыг
мэдэхгүй тул «кодоо хэлэхэд бид олно» гэсэн мөр зөвхөн манай шугамын дор.

### Нэг үнэ, нэг эх сурвалж
Хүргэлтийн үнэ **дөрвөн** газар бичигдсэн байсан: Sheet, кодын нөөц жагсаалт,
барааны `.trust` badge, бодлогын хуудас. Sheet дээр үнэ солиход хоёр хуудас
хуучин тоог **үйлчлүүлэгчид харуулсаар** байв. Одоо `deliveryOptions()` /
`deliverySummary()` хоёулаа Sheet-ээс уншина. Badge нь дүнгийн муж
(`6,000₮–12,000₮`) харуулна — шуурхай сонголт нь өмнө нь огт дурдагддаггүй
байсан.

Бодлогын хуудасны `body` нь **функц байж болно** (`renderPolicy` шалгана) —
Sheet-ээс тоо авах хуудсууд ингэж бичигдэнэ.

### Хугацааны гурван утга зөрчилддөг байсан
«5-7 хоногт» (Sheet), «24 цагийн дотор» (Sheet), «08:00–12:00» (код) —
гурвуулаа өөр зүйлийн тухай байсан ч зөрчил мэт уншигддаг байв. Бодлогын
хуудсыг зассан: **хүлээх хугацаа бол барааных**, 08:00–12:00 бол
**хүргэлтийн цонх**, бараа агуулахад ирсний дараах.

### Барааны тайлбар олон мөртэй
Sheet дэх `desc` нь `
`-ээр тусгаарлагдсан — гарчиг, дараа нь мөр тутамд
нэг давуу тал. `esc()`-ээр ганц `<p>`-д хийхэд HTML мөр таслалтыг залгидаг
тул 8 өгүүлбэрийн ханан догол мөр болдог байв. `descBlock()` мөр бүрийг
тусад нь, «—»-ийн өмнөх хэсгийг тодоор гаргана.

### Гарчиг ба canonical route-ыг дагана
`setHead(title, path)`. Бүх хуудас «Starshopping» гэж нэрлэгддэг байсан.
Барааны canonical нь `/p/<slug>/` карт хуудас руу заана — **зөвхөн латин
slug-тай бараанд** (`CARD_SLUG`), учир нь картыг зөвхөн тэдэнд үүсгэдэг.

### Серверийн үнийн шалгалт
Үнийг **сервер** боддог, браузерын явуулсан тоог хүлээж авдаггүй — одоо
intake (`intake_order()`), өмнө нь `Code.gs`-ийн `computeOrder_()`. Өмнө нь
349,000₮-ийн барааг 1₮-өөр захиалах боломжтой байсан. Сайт үнэ **илгээдэггүй**;
илгээх болговол энэ хамгаалалтыг сулруулна.

### Нөөц: хоосон ≠ 0
`Нөөц` хуудасны `Агуулахад буй`:
- **хоосон** = хязгаар тавиагүй, үргэлж зарагдана
- **0** = дууссан, товч хаагдана

Шинэ мөр **хоосноор** үүсэх ёстой. Нэг удаа 0-оор үүсгээд бүх бараа
"ДУУССАН" болсон.

### Хуваалцсан холбоос — зочныг хэзээ ч нүүр рүү шиднэ гэж байхгүй
Каталог **гурван эх сурвалжаас** ирнэ:

1. `localStorage` (`ss_catalog_v3`) — өмнөх зочилтын хуулбар, шууд зурагдана
2. `data/catalog.json` — өөрийн домэйн дээр, 20 минут тутам шинэчлэгддэг,
   **0.5 секундэд** ирнэ
3. Apps Script feed — жинхэнэ өгөгдөл, **хэмжсэнээр 3.7–5.1 секунд**

⚠️ **2026-08-20-нд зассан хоёр эвдрэл.** Эзэн "барааны холбоосоор ороход
маш удаад нүүр хуудас руу очлоо" гэж мэдээлсэн. Хэмжиж давтсан:

| Юу байсан | Хэмжилт |
|---|---|
| Хуучин кэштэй зочин барааны холбоосоор орох | 0.9с-т **нүүр хуудас**, 4 секунд тэнд сууж, 4.8с-т буцаж бараан дээр |
| Одоо | 1.2с-т **шууд бараан дээр**, нүүр рүү огт очихгүй |

Шалтгаан ба дүрэм:

- **`goHome()` барааны холбоосыг хөндөхгүй.** Хуучин код бараа олдоогүй үед
  `location.hash = "#/"` хийж, hero-гийн анимацийг үзүүлээд, 4 секундын
  дараа `missedRoute`-оор буцаадаг байв. Одоо зочин **барааны хуудсан дээрээ
  үлдэж**, `renderPending()` (араг ясан төлөв) харагдана. `goHome` зөвхөн
  ноорхойгүй `#/order`, `#/done`-д үлдсэн.
- **`data/catalog.json`-г кэш байсан ч ҮРГЭЛЖ ав.** Хуучин код кэш байвал
  үүнийг огт татдаггүй байсан — яг л энэ дэлгүүрийн гол зочин (хэдэн өдрийн
  өмнө орж үзсэн, өнөөдөр reel хараад дарсан) кэш нь тэр бараа гарахаас
  өмнөх учир юу ч мэдэхгүй, зөвхөн 5 секундын feed-г хүлээдэг байв.
  `routeUnresolved()` нь одоогийн хаяг тайлагдаж байгаа эсэхийг хэлнэ.
- **`renderMissing()` (бараа олдсонгүй) зөвхөн `liveLoaded` үнэн үед.**
  Хуучин `rescueTimer` нь 4 секундэд `liveLoaded = true` гэж **худал**
  тавьдаг байсан — байгаа барааг "олдсонгүй" гэж хэлэх эрсдэлтэй.
- **Feed-д хугацааны хязгаар (15 сек, `AbortController`).** Сул сүлжээтэй
  утас хүсэлтийг минутаар барьж чадна; тэр хооронд slug байхгүй юу, эсвэл
  зүгээр хоцорч байна уу гэдгийг ялгах аргагүй.

Энэ логикийг арилгаж болохгүй — үйлчлүүлэгч бараа руугаа хүрэхгүй бол бүх
зар дэмий.

### Барааны бүх зургийг зэрэг татахгүй
Барааны хуудас Drive-аас **5 × ~500KB = 2.5MB** зургийг зэрэг татдаг байсан.
`loading="lazy"` ямар ч тус болоогүй — зургууд track дотор хажуу хажуугаараа
байрладаг тул хөтөч бүгдийг нь "ойрхон" гэж үзээд татна. Утсан дээр энэ л
"маш удаж байна" гэдэг нь.

Одоо `buildFrame()` зөвхөн **эхний зурагт `src`** тавьж, үлдсэнийг
`data-src`-д барина. `showFrame()` нь очих зураг ба хөршийг нь
(`hydrateFrame`) сэрээнэ. Хэмжсэн: эхний зурагдалтад **5 биш 1** хүсэлт.

- Жагсаалтын жижиг зураг (`.frame`, ~150px өргөн) **`w400`** (207KB),
  барааны галерей `w1200` (505KB). `imageUrl(raw, size)`.
- ⚠️ `imageUrl`-г `.map(imageUrl)` гэж **шууд дамжуулж болохгүй** — `map` нь
  индексийг хоёр дахь аргумент болгож өгдөг тул `sz=w0` болно. Заавал
  `.map((u) => imageUrl(u, 1200))`.
- Эх зураг өөрөө ~679px өргөн тул `w800` ба `w1200` **ижил файл** буцаана.

### Барааны зураг өөрийн домэйн дээр — Drive бол зөвхөн нөөц
2026-08-20-нд хийсэн. `tools/build-og.py` нь Sheet дэх Drive зураг бүрийг
татаж, **WebP болгож `img/<driveId>-<өргөн>.webp`** нэрээр хадгална.
Хоёр өргөн: `400` (жагсаалтын жижиг зураг) ба `1200` (галерей).

Хэмжсэн үр дүн:

| | Drive PNG | Өөрийн домэйн WebP |
|---|---|---|
| Галерейн зураг | 505KB | **30–45KB** |
| Жагсаалтын зураг | 207KB | **15–22KB** |

Ингэснээр эгзэгтэй замаас гуравдагч сервер бүрмөсөн гарсан (§3-ын
«Гуравдагч серверийг эгзэгтэй замд оруулахгүй» дүрэмтэй нийцэв).

**Нөөц зам заавал байх ёстой.** Толь 20 минут тутам шинэчлэгддэг тул саяхан
нэмсэн зурагт толь байхгүй байж болно. Тиймээс `<img>` бүр `data-fallback`-д
Drive-ын хаягийг барина, `script.js` доторх **нэг** capture-фазын `error`
сонсогч байхгүй тохиолдолд чимээгүй Drive руу сольдог. Хэрэглэгч эвдэрсэн
зураг хэзээ ч харахгүй, шинэ бараа build хүлээхгүй.

- Сонсогч **capture фазад** байх ёстой — зургийн `error` дээшээ bubble
  хийдэггүй.
- `delete img.dataset.fallback` — нэг л удаа оролдоно, давталт үүсэхгүй.

**Дэмий дахин бүтээхгүй.** `img/manifest.json` нь id бүрд Drive-ын
`Content-Length`-ыг хадгална. Тухайн бүрд **HEAD** хүсэлт (биегүй, хямд)
явуулж харьцуулна. Хоёр дахь ажиллалт: **0 дахин бүтээв, 14 секунд**.
Drive хэмжээгээ хэлэхгүй бол байгаагаа хэвээр үлдээнэ — 20 минут тутам
бүх зургийг дахин татахаас сэргийлнэ.

⚠️ **`pages.yml`-д `img` байх ёстой** — `git status --porcelain` ба
`git add` хоёуланд. Байхгүй бол зураг үүсээд commit хийгдэхгүй.

⚠️ **Эрүүл мэндийн шалгалт толийг харна** (`health-check.py` → `mirror_urls`).
Толь deploy хийгдээгүй бол сайт ажилласаар байх тул **алдаа гэж үзэхгүй**,
зөвхөн тэмдэглэнэ. Эс тэгвэл чимээгүйхэн Drive рүү буцаад удаашрахыг хэн ч
мэдэхгүй.

Байрлал: 9 зураг × 2 өргөн = 548KB. 50 бараа × 5 зураг ≈ 15MB — GitHub
Pages-ийн хязгаараас хол доор.

### Дахин зурсны дараа `startFrames()`-ыг ЗААВАЛ дуудна
`paint()` нь feed ирэхэд барааны хуудсыг дахин зурдаг ч `startFrames()`-ыг
дуудахаа мартсан байв. Дахин зурахад timer-ийн хөдөлгөж байсан элементүүд
солигдоно — үр дүнд нь **зургийн автомат гүйлгэлт бүр удаа үхдэг байсан**.
Амьд сайт дээр хэмжсэн: 20.8 секунд, зураг нэг ч удаа солигдоогүй. Эзэн
үүнийг мэдээлсэн.

Дүрэм: `renderCategory()` эсвэл `renderProduct()` дуудсан бол ард нь
`startFrames(views.category | views.product)`.

### Барааны галерейн удирдлага — зочны гарт
Эзний хүсэлт: "үйлчлүүлэгч хүссэн үедээ хөдөлгөөд хүссэн зургаа хараг."
Барааны галерейд дөрвөн зам нэмсэн, бүгд нэг `showFrame()`-оор явна:

- `‹` `›` сум (`.gnav`, зургийн дотор, 40×40)
- цэг дээр дарах (`.pdot`-д `::after`-оор томруулсан дарах талбай — 3px
  зураас бол хуруунд боломжгүй)
- утсан дээр **шудрах** (40px-ээс дээш)
- гарын сум (нэг л удаа бүртгэсэн listener — дахин зурах бүрд бүртгэвэл
  хуучин галерейг амьд барина)

Автомат гүйлгэлт хэвээр, **гэхдээ зочин нэг л хүрмэгц зогсоно**
(`dataset.manual = "1"`). Хуруун доор өөрөө хөдөлдөг галерей нь огт
хөдөлдөггүйгээс дор.

### `CACHE_KEY` — Sheet-ээс зураг/категори хассан бол өсгө
`localStorage`-д хуучин хуулбар үлдвэл хасагдсан зураг зочин бүрийн эхний
хормыг эзэлсээр байна. Sheet дээрх зураг эсвэл категори **хасагдсан** үед
`CACHE_KEY`-г өсгө (`v3` → `v4`) ба `data/catalog.json`-г мөн зэрэг
шинэчил. Нэг удаа муурны зураг Sheet-ээс хасагдсан ч хэдэн өдөр
"заримдаа" гарсаар байсан.

### Зургийн хаяг — Drive-ын **файлын** холбоос
`imageUrl()` зөвхөн дараах хэлбэрийг хөрвүүлнэ:
`/file/d/АЙДИ/...`, `open?id=`, `uc?id=` → `thumbnail?id=АЙДИ&sz=w1200`

Ажиллахгүй зүйлс — эзэн бүгдийг нь туршсан:
- **Файлын нэр** (`767242790_..._n.png`) — нүдэнд Insert → Image in cell
  хийхэд ингэж ирдэг. Зураг бүгд эвдэрнэ.
- **Хавтасны** холбоос (`/drive/folders/...`) — хөрвөхгүй
- Drive дээр "Anyone with the link" болгоогүй файл — 403

---

## 4. Deploy — юу хэзээ

| Юу өөрчлөгдсөн | Хийх зүйл |
|---|---|
| `index.html`, `style.css`, `script.js`, `assets/`, `tools/` | `git push origin main` → GitHub Actions автоматаар (~40 сек) |
| **Шинэ бараа, үнэ, зураг, нөөц** | n8n маягт **13 · Бараа бүртгэх** бөглөнө (§17) — дараагийн ачаалалтад тусна, холбоосын карт ~20 минутад |
| `apps-script/Code.gs` | Эзэн гараар: код хуулах → `setup` Run → `Deploy` → **Manage deployments** → ✏️ → **New version** → Deploy |
| Sheet дээрх өгөгдөл (хүргэлт, категори, багц, сэтгэгдэл) | **Юу ч хийхгүй** — дараагийн ачаалалтад тусна |
| Захиалгын логик (үнэ, нөөц, давхардал, мэдэгдэл) | Эзний n8n / Supabase тал — энэ repo-д биш |

**Apps Script deploy-г зөвхөн эзэн хийж чадна** (түүний Google эрх шаардлагатай).
Код өөрчилсөн бол файлыг `SendUserFile`-аар явуулж, алхмуудыг сануул.

Deploy хийсэн эсэхийг шалгах: feed-ээс шинэ талбар ирж байгаа эсэхийг
`curl`-ээр үзнэ.

---

## 5. Sheet-ийн бүтэц

⚠️ **2026-09-18-аас бараа Sheet-д биш, Supabase-д** (§17). Supabase-д ядаж нэг
бараа байгаа үед Sheet-ийн `Products` хуудасны бараа сайт дээр **гарахгүй** —
зөвхөн ижил slug-тай мөрөөс `colors, colorImages, sizes, sizePrices,
sizeImages, leadTime, leadNote` зээлж авна (Supabase-д эдгээр багана байхгүй
хүртэл). `Нөөц`-ийн тоо ч Supabase-ийн барааны хувьд **хүчингүй** — intake
`stock_qty`-гаар шалгадаг. Sheet-ийн үлдсэн үүрэг: **дэлгүүрийн мэдээлэл
(банк, утас), хүргэлтийн сонголтууд, Categories, Bundles, Reviews, Заавар.**

Дэлгэрэнгүйг Sheet дэх **`Заавар`** хуудаснаас, эсвэл `Code.gs` доторх
`PRODUCT_NOTES` тогтмолоос үз. Товчхон:

- **Products** — `slug, category, name, desc, price, discount, sizes,
  sizePrices, colors, colorImages, sizeImages, image1-5, stock, active,
  Хүргэлтийн хугацаа, Хүргэлтийн тайлбар`
- **Нөөц** — `slug, Бараа, Агуулахад буй, Захиалагдсан, Боломжит`
  (Захиалагдсан = Orders+Archive дээрх SUMIF, цуцлагдсаныг тооцохгүй)
- **Bundles** — `product, qty, price, label, active` (2+1 маягийн багц)
- **Reviews** — `product, name, text, rating, image, active`
  (`product` хоосон = бүх бараанд)
- **Orders / Archive** — 16 багана, төгсгөлд нь `SKU` (нөөцийн тооллого үүгээр)

**Дүрэм:** нүд хоосон бол тухайн зүйл сайт дээр **огт харагдахгүй**
(хямдрал, өнгө, size, багц бүгд ийм).

### Хүргэлтийн хоёр багана
| Багана | Хаана | Хоосон бол |
|---|---|---|
| `Хүргэлтийн хугацаа` | **Том цагаан** мөр. Богино байх ёстой | `Өглөөний 08:00–12:00` |
| `Хүргэлтийн тайлбар` | Доор нь **жижиг бүдэг**. Урт байж болно | `Захиалга баталгаажсаны дараа хүргэнэ.` |

Хоёр хэсэг болгосон шалтгаан: эзэн урт тайлбараа хугацааны нүдэнд бичихэд
том цагаан мөр бүхэл догол мөр болж, нүд буух цэг алга болсон.

⚠️ **Давхардсан баганын урхи.** `setup()` ажиллахад хуучин баганын хажууд
шинийг нэмж, `Хүргэлтийн хугацаа` **хоёр удаа** үүссэн. `rowsOf()` мөрийг
нэрээр нь уншдаг тул сүүлчийн (хоосон) багана өмнөхийг дардаг —
бичсэн бичвэр огт гарахгүй болно. Sheet дээр багана давхардсан эсэхийг
шалга, илүүг нь устга.

**Хуучин `Горим`/`Test` багана 2026-08-10-нд бүрмөсөн хасагдсан.** Тэр нь
дөрвөн зүйлийг зэрэг хийдэг байсан; одоо:

| Хуучин Test юу хийдэг байсан | Одоо |
|---|---|
| Хугацааг "7–14 хоног" гэж харуулах | `Хүргэлтийн хугацаа` баганад өөрөө бичнэ |
| Нөөц шалгахгүй | `Нөөц`-ийн `Агуулахад буй`-г **хоосон** орхи |
| Төлбөрийн сонголт нуух | Хассан — төлбөр **үргэлж харагдана** (эзний шийдвэр) |
| Sheet-д `Тодорхойгүй (Test)` бичих | Хассан — тест захиалга ялгагдахгүй |

⚠️ Агуулахад байхгүй, гадаадаас захиалах бараанд `Агуулахад буй`-г **заавал
хоосон** орхи. `0` бичвэл бараа "ДУУССАН" болж хаагдана — Test горим үүнийг
өмнө нь тойрч гардаг байсан, одоо тойрох зүйл алга.

---

## 6. Хэрхэн шалгах

**Таамаглаж болохгүй — хэмж.** Ажилласан арга:

```bash
# локал сервер
cd C:/Users/ariun/Documents/Starshopping-web
python -m http.server 8777 --bind 127.0.0.1
```

Дараа нь browser tool-оор `http://127.0.0.1:8777/` нээж `javascript_tool`-оор
хэмжинэ. Заавал анхаарах хоёр урхи:

1. **`scroll-behavior: smooth`** — `scrollTo(0,0)` нь анимацитай. Хэмжихдээ
   `window.scrollTo({top:0, behavior:'instant'})` ашигла.
2. **scrub-ийн 0.5 сек хоцролт** — гүйлгээд шууд уншвал хуучин утга гарна.
   Timeline-г шууд жолоод: `heroPin.animation.progress(p).pause()`.

Zoom эсэн мэнд эсэхийг батлах:
```js
const pin = ScrollTrigger.getAll().find(t => t.vars.pin && t.trigger?.id === 'hero');
const img = document.querySelector('.hero__cam img');
window.scrollTo({top:0, behavior:'instant'}); ScrollTrigger.update();
pin.animation.progress(1).pause();
const r = img.getBoundingClientRect();
console.log('scale', getComputedStyle(img).transform,
            'lens off centre Y', Math.round(r.top + r.height*0.875 - innerHeight/2));
// хүлээгдэж буй: scale 11, зөрүү ~0
pin.animation.progress(0).resume();
```

Хар зай үүсээгүй эсэх:
```js
const hero = ScrollTrigger.getAll().find(t => t.vars.pin && t.trigger?.id === 'hero');
const cats = ScrollTrigger.getAll().find(t => t.vars.pin && t.trigger?.id === 'cats');
console.log('pin gap', Math.round(cats.start - hero.end));  // 0 байх ёстой
```

Шалгах хэмжээнүүд: **1280×800** (desktop), **390×844** (утас),
**390×600** (Instagram-ийн дотоод браузер — энд л ихэнх алдаа гардаг).

Deploy-ийн дараа `starshopping.mn` дээр дахин шалга.

---

## 7. Өмнө хийсэн алдаанууд — давтахгүй

| Алдаа | Тайлбар |
|---|---|
| **Library vs Web app URL** | Эзэн `/macros/library/d/...` явуулдаг. Хэрэгтэй нь `/macros/s/.../exec`. Байгаа deployment-ээ засвал **хаяг өөрчлөгддөггүй** — эхлээд хуучин хаягийг шалга. |
| **`getUi().alert()`** | Скрипт засварлагчаас Run хийхэд цонх Sheet дээр гарч, хэн ч хариулахгүй → 6 минут гацна. `Logger.log` ашигла. |
| **`clearProps: "all"`** | CSS custom property-г арчина. Зорилтот property л цэвэрлэ. |
| **Нөөцийг 0-оор үүсгэх** | Бүх бараа хаагдана. Хоосноор үүсгэ. |
| **Гараар нэмсэн нөөцийн мөр** | `Боломжит` томьёогүй байвал 0 гэж уншигдана. Одоо код өөрөө боддог (`orderedBySku_`). |
| **Placeholder зурагт итгэх** | "Тунгалаг" гэж татсан PNG-д checkerboard **бодит пиксэлээр** шатсан байсан. `rembg` ашигла. |
| **CRLF vs LF** | `git rebase` хийсний дараа ажлын хуулбар CRLF болдог (autocrlf). Патч бичихдээ `io.open(...).read()` (universal) уншиж, бичихдээ файлын **анхны** төгсгөлөөр буцааж бич. Эс тэгвэл хайлт таарахгүй. |
| **Heredoc доторх `\\n`** | Bash heredoc нэг давхар escape иддэг тул Python-д `\n` болж, файлд **жинхэнэ мөр таслалт** бичигддэг — regex эвдэрнэ. Урт патч бичихдээ Write хэрэгслээр файл үүсгэ. |
| **Template literal доторх backtick** | HTML тайлбарт `` `novalidate` `` гэж бичсэн нь JS мөрийг тасалж, бүх script унасан. Тайлбарт backtick хэрэглэхгүй. |
| **`parts.find(([, v]) => !v.value)`** | `v` бол мөр, `.value` байхгүй → үргэлж эхний талбарыг буруу гэж заана. Тестээр илэрсэн — гараар шалгаж бай. |
| **Smooth scroll хэмжих** | Хуудас compositing хийхгүй байхад smooth гүйлт **0 пиксел** хөдөлнө. «Гүйлт ажиллахгүй байна» гэж дүгнэхээсээ өмнө instant-аар шалга. |
| **`.map(imageUrl)`** | `map` нь индексийг 2 дахь аргумент болгож дамжуулна. `imageUrl(raw, size)` болсноос хойш энэ нь `sz=w0` гэсэн үг. Лямбдагаар боо. |
| **Хэмжилтийн артефакт** | Нэг удаа "zoom эвдэрсэн" гэж дүгнэсэн ч smooth-scroll + scrub lag байсан. Дүгнэхээсээ өмнө хэмжилтийн аргаа шалга. |
| **Hash солиод "хүйтэн ачаалалт" гэж хэмжих** | Hash солиход хуудас дахин ачаалагддаггүй, `DB` дүүрсэн хэвээр тул холбоосын алдаа **нуугдана**. `localStorage` цэвэрлээд `location.reload()` хий. Энэ артефактаас болж "алдаа алга" гэж нэг удаа буруу дүгнэсэн. |
| **5 секундын автомат солилт** | Категори 5 сек тутам өөрөө солигддог. Хэмжихийн өмнө `stopCycle()` дуудахгүй бол өмнөх утга гарч ирнэ. |
| **Карт хуудас query-г хаях** | `location.replace("<тогтмол хаяг>")` нь `?ref=`-ийг устгана. Redirect бичихдээ `location.search`-ийг үргэлж дамжуул. |
| **Баримтад итгээд хариуны хэлбэрийг таамаглах** | Intake-ийн баримт `error: "…"` гэсэн ч бодит хариу `errors: ["…"]` + HTTP 400 байв. Гадны API-г эхлээд **нэг удаа дуудаж** хэлбэрийг нь хар. |
| **Нуугдсан pane дээр `IntersectionObserver` хэмжих** | Хөтчийн цонх зурагдаагүй үед observer callback ирэхгүй — «самбар гарахгүй байна» гэж буруу дүгнэх шахсан. Screenshot авч frame гаргасны дараа унш. |
| **Кэштэй tab дээр «зураг татагдаагүй» гэж хэмжих** | Өмнөх ачаалалтын memory cache-ээс hero зураг `link` initiator-тай гарч ирсэн. Хүйтэн хэмжилтийг **өөр origin** (`127.0.0.1` ↔ `localhost`) дээр хий. |
| **Туршилтын DOM өөрчлөлтийн дараа hero хэмжих** | `DB`-г гараар сольж `renderCategories()` дуудсаны дараа линзний зөрүү `158px` гэж гарсан ч цэвэр ачаалалт дээр `0` байсан. Hero-г үргэлж **шинэ ачаалалт** дээр хэмж. |

---

## 8. Ажлын хэв маяг

Эзэн нь техникийн бус, монголоор ярина. Хүлээж буй зүйл:

- **Монголоор хариул.**
- **Дизайныг хамгаал.** Zoom, аварга үсэг, хар/цагаан загварыг эвдэхгүй.
  "Загвараа олох гэж зовсон" гэж хэлсэн.
- **Гацалт гарч болохгүй** — үйлчлүүлэгч шууд алдагдана.
- **Таамаглахгүй, хэмж.** Бичлэг/зураг илгээвэл кадр задалж (`ffmpeg`),
  амьд сайтыг хэмжиж оношил.
- **Худал ажилладаг дүр эсгэхгүй.** Дуусаагүй бол дуусаагүй гэж хэл.
- **Зохиомол сэтгэгдэл бичихгүй.** Reviews-д зөвхөн жинхэнэ хэрэглэгчийн үг.
- Бизнесийн шийдвэр (багцын үнэ, маржин) эзэнийх — код бэлдээд өг.

---

## 9. Дараа хийх зүйлс

**Эзэн хийх (сайт хүлээж байна) — бүгд Sheet дээр, код талд хийх зүйл алга:**
- `Huwtsas-hadgalah-sags`-ийн `Нөөц` → `Агуулахад буй` = **0** байна, тиймээс
  тэр бараа сайт дээр "ДУУССАН" болж товч нь хаалттай. Агуулахад байхгүй
  боловч гадаадаас захиалдаг бол энэ нүдийг **хоосон** болго (§5-ыг үз)
- `Хүргэлтийн тайлбар` нүдэнд үсгийн алдаа: `агуулхад` → **`агуулахад`**,
  `хүргэлтээр очин` → **`хүргэлтээр очно`**. Мөн `😊` emoji ба давхар зайг
  хас — сайтын бусад хэсэгт нүүр emoji огт байхгүй, ганцаараа тохирохгүй
  харагдана. (Эдгээр нь **кодод биш**, зөвхөн Sheet-д байна — repo даяар
  хайж баталсан.)
- `image1` нүдэнд **цэвэр бүтээгдэхүүний зураг** тавь. Одоо эхний нүдэнд
  нийлүүлэгчийн хятад бичигтэй зар байна. Захиалгын хуудас зочны үзэж байсан
  зургийг харуулдаг болсон ч, шинэ зочин эхлээд `image1`-ийг хардаг
- `colorImages` нүдэнд «Усан ногоон»-д харгалзах зургийн холбоос тавь —
  өнгө сонгоход зураг солигдох логик бэлэн, зөвхөн өгөгдөл дутуу
- `Bundles`, `Reviews` хуудас хоосон
- Тест захиалгуудыг `Orders`-оос устгах
- Бодлогын хуудсуудыг уншиж батлах (буцаалт 1–3 өдөр, хүргэлт 08:00–12:00)

**Хийгдсэн (2026-08-20-нд шалгаж баталсан):**
- Sheet дэх зургууд Drive-ын **файлын** холбоос болсон — feed дээр зөв ирж байна
- `Huwtsas-hadgalah-sags` slug цэвэр, хоосон зайгүй
- Холбоосын урьдчилсан харагдац (OG) бүрэн ажиллаж байна — §11-ийг үз

**Эзэн хийх — шинэ backend (2026-09-17/18):**
- ✅ **Хийгдсэн (2026-09-18):** 2 бараа маягт 13-аар Supabase-д орсон; `web_products`
  anon key-д ажилладаг болсон (эзэн SQL-ээр функцийг `security definer` болгосон —
  өмнө нь anon-д `[]` буцаадаг байсан, учир нь `products`-ын RLS anon-д юу ч
  өгдөггүй); амьд сайт Supabase-аас уншиж, захиалгад UUID илгээж байгаа нь
  хэмжигдсэн; n8n «02»-ын `SLUG_TO_ID` гүүр устгагдсан. Шинэ функц нэмэгдвэл мөн
  `security definer` + `grant execute … to anon` хэрэгтэйг сана — функц нэрээр
  олдохгүй бол параметртэй, `pg_proc`-оос `oid::regprocedure`-ээр ол.
- Өртөг (`cost_mnt`) 2 бараанд түр = зарах үнэ (ашиг 0). Бодит өртөг мэдэгдмэгц
  маягт 13-аар product_id-г буулгаж бүх талбарыг дахин бөглөж зас.
- `Huwtsas-hadgalah-sags` Supabase-д `stock_qty = 0` → сайт дээр «Дууссан», intake
  татгалзана. Гадаадаас захиалдаг бол маягт 13-аар нөөцийг **хоосон** болго.
- ⚠️ **n8n-ийн оношилгооны webhook** нэвтрэлтгүй байсан (2026-09-18-нд илрүүлсэн). Backend чат хариуцаж хааж байгаа. **Түүний хаяг, параметрийг энэ нийтийн repo-д бичихгүй** — хэрэгтэй бол эзний `_bus/` хавтаснаас.
- Intake-д хүргэлтийн төрөл/төлбөр, өнгө/хэмжээний талбар байхгүй — одоо
  хаягийн мөрийн `[…]` дотор явж байгаа. Хэрэгтэй бол база талд нэм.
- Захиалга Sheet-д орохоо больсон тул `Нөөц`-ийн «Захиалагдсан» тоо, Apps
  Script-ийн Telegram мэдэгдэл (§15) **шинэ захиалгад ажиллахгүй** — мэдэгдлийг
  n8n талд хий.

**Хийхгүй гэж шийдсэн (2 бараа, дуусгасан тест 0):** сагс, купон, онлайн
төлбөр, захиалгын статусын хуудас.

**Код талд:**
- 10 захиалга тутмын имэйл (код бэлэн, 10 дахьд ажиллана)
- Домэйн авбал GitHub Pages-д холбоно. Энэ нь `fb:app_id` анхааруулгыг мөн
  арилгана (§14)

---

## 10. Хэрэгслүүд

Энэ машин дээр суулгасан: `ffmpeg` (бичлэг задлах), Python + `rembg`
(зургийн дэвсгэр арилгах) + `Pillow`, `gh` CLI (хоёр account: `starshopping-mn`
идэвхтэй, `ariunboldagency-commits` хуучин repo-д).

`gh auth switch -u <нэр>` — account солих. Хуучин repo руу түлхэхэд хэрэгтэй.

---

## 11. Холбоосын карт (OG) — автомат

**Зорилго:** Reel-ийн тайлбар эсвэл мессежээр илгээсэн холбоос **дарахаасаа
өмнө** барааны зураг, нэр, үнэтэй харагдах.

### Хэрхэн ажилладаг
`.github/workflows/pages.yml` дотор `tools/build-og.py` ажиллана:

- **20 минут тутам** (cron), push бүрд, мөн гараар (`workflow_dispatch`)
- Sheet-ийн feed-ээс идэвхтэй бараа бүрийг уншина
- `p/<slug>/index.html` — OG шошготой карт хуудас
- `og/<slug>.jpg` — эхний зургийг татаж 1200×630 болгож хар дэвсгэр дээр
  байрлуулна. **Өөрийн домэйн дээр** байрлана: робот Drive руу очих
  шаардлагагүй болно.
- Өөрчлөлт байвал л commit + deploy хийнэ

### Эзэн юу ч хийхгүй
Sheet-д бараа нэмэхэд карт **өөрөө үүснэ**. Ганц зүйл: карт бэлэн болоход
~20 минут (бараа өөрөө сайт дээр шууд гарна). Яаралтай бол GitHub → Actions →
"Deploy to GitHub Pages" → **Run workflow**.

### Эвдэрч болохгүй зарчим — сайтыг хэзээ ч унагаахгүй
Скрипт **бүх алдааны замд `exit 0`** буцаана, workflow-д
`continue-on-error: true` тавьсан. Тусгайлан шалгасан 19 тохиолдол:

| Нөхцөл | Зан төлөв |
|---|---|
| Sheet хариу өгөхгүй | Байгаа картуудыг **хөндөхгүй** |
| Feed хоосон ирэх | Байгаа картуудыг **хөндөхгүй** (буруу deploy байж магадгүй) |
| Зураг татагдахгүй | Хуудас зурагтгүйгээр үүснэ (`summary` карт) |
| Барааны нэрэнд `"` эсвэл HTML | Escape хийгдэнэ |
| Slug `../` эсвэл кирилл/зайтай | **Алгасна**, мод дундаас гарахгүй |
| Бараа Sheet-ээс хасагдах | Картыг нь устгана |
| `active` = FALSE | Карт үүсгэхгүй |

### Урт категорийн нэр
Жагсаалтын нэрс `--fit` гэсэн хувиар автоматаар багасдаг (`fitRail()`).
"ХҮҮХДИЙН ХЭРЭГСЭЛ" нь 150px багананд 199px өргөнтэй байсан тул эхний 28px
нь тасарч байсныг ингэж зассан. Гурван зүйлийг санах:

- Хувь нь **бүх нэрэнд нэг адил** тавигдана. Нэр бүрийг өөрөөр багасгавал
  эвдэрсэн мэт харагдана.
- Өргөн дэлгэцэд багана (210px) хязгаарлана. Утсан дээр **зурвасын өргөн**
  хязгаарлана — хөрш нэрсээр хэмжиж болохгүй, тэд бүдгэрч маскаар уусдаг
  тул тэднээс болж голын нэрийг жижигрүүлэх нь буруу. Нэг удаа ингэж
  хэмжээд утсан дээр үсэг 8.3px болж уншигдахаа больсон.
- Доод хязгаар 0.8. Түүнээс доош унавал багана өргөсгөх хэрэгтэй.

### Slug-ийн дүрэм — энэ л ганц шаардлага
Карт `p/<slug>/` хавтас болдог тул slug нь **латин үсэг, тоо, зураас** байх
ёстой (`huvtsas-hadgalah-sags`). Кирилл, хоосон зай, налуу зураастай slug
картгүй үлдэнэ — сайт дээр бараа хэвийн ажиллана, зөвхөн карт гарахгүй.
Action-ы лог дээр аль нь алгассаныг бичнэ.

### Зар явуулсны дараа slug БИТГИЙ СОЛЬ
Slug бол барааны байнгын хаяг. Сольвол хуучин холбоос үхнэ — нэг удаа
`Huwtsas hadgalah shiid` → `Huwtsas-hadgalah-sags` болоход өмнөх бүх
холбоос нүүр рүү буцдаг болсон. Үнэ, зураг, тайлбар, нөөцийг чөлөөтэй
солиж болно.

### Pages-ийн deploy хязгаар
GitHub Pages цагт ~10 build л зөвшөөрдөг. Тиймээс **цагийн ажиллалт
өөрчлөлт олоогүй бол deploy алгасдаг**. Энэ `if:` нөхцөлийг арилгавал
20 минут тутам дэмий deploy хийж хязгаарт хүрнэ.

---

## 12. Эрүүл мэндийн шалгалт — цаг тутам

`.github/workflows/health.yml` → `tools/health-check.py`. Цаг тутам (мөн
гараар) ажиллаж, **үйлчлүүлэгчийн хүрдэг зүйлсийг** шалгана:

- Сайт хариу өгч байна уу, нүүрний OG шошго бүрэн үү
- Sheet-ийн feed хариу өгч, бараатай ирж байна уу
- Офлайн нөөц feed-тэй таарч байна уу (зөрвөл зөвхөн тэмдэглэнэ — 20 минутын
  дотор өөрөө засагдана, алдаа гэж үзэхгүй)
- **Бараа бүрд:** картын хуудас нээгдэж байна уу, OG шошго бүрэн үү,
  **картын үнэ Sheet-ийнхтэй таарч байна уу**, картын зураг ачаалагдаж
  байна уу, барааны бүх зураг ачаалагдаж байна уу

Алдаа олдвол ажиллалт улаан болж, **GitHub эзэнд имэйл илгээнэ**.

### Яагаад гуравдагч хэрэгсэл (UptimeRobot) биш вэ
Эзэн UptimeRobot-д бүртгүүлсэн ч түүний нэвтрэх мэдээлэл/API түлхүүрийг
Claude авахгүй. Мөн UptimeRobot зөвхөн "сайт хариу өгч байна уу" гэдгийг л
хардаг — үнэ зөрсөн, зураг эвдэрсэн, картын хуудас алга болсныг мэдэхгүй.
Эдгээр нь яг мөнгө алддаг эвдрэлүүд.

### Дохио дэмий хашгирахгүй байх
Шалгалт бүр **3 удаа давтаж** үзсэний дараа л алдаа гэж үзнэ. Нэг удаагийн
сүлжээний саатал алдаа биш. Дэмий дохио өгдөг систем удалгүй үл тоомсорлогдоно,
тэр нь дохиогүй байснаас дор.

### Алдаа мэдэрдэг эсэхийг батлах
`tools/` доторх шалгалтыг зөвхөн "ногоон гарлаа" гэж бүү итгэ — **эвдээд**
үз. Таван хувилбарыг зориуд эвдэж шалгасан: сайт унасан, feed унасан, зураг
эвдэрсэн, картын үнэ зөрсөн, картын хуудас алга. Тавуулаа мэдрэгдсэн.

---

## 13. Лого ба брэндийн өнгө

**Эх файл:** `C:\Users\ariun\OneDrive\Pictures\ЛОго\1.png` (2000×2000)

Логоноос хэмжсэн жинхэнэ өнгө: дэвсгэр `#f3f2ee`, бэх `#3c1f15`.
Сайт дээр **тодруулсан** хувилбарыг хэрэглэнэ — эзэн логоных нь хос
"баргар" гэж үзсэн:

| | Логонд | Сайтад |
|---|---|---|
| Дэвсгэр | `#f3f2ee` | `--bg: #fbf7f0` |
| Бэх | `#3c1f15` | `--fg: #45241a` |

### Хаана юу хэрэглэгддэг
| Файл | Юу | Хэрхэн үүссэн |
|---|---|---|
| `assets/logo-mark.png/.webp` | Толгой, хөл дэх од тэмдэг | Хар дэвсгэрт зориулсан **саарал** байсныг бэхийн өнгөөр дахин будсан |
| `assets/favicon.png` | Хөтчийн таб | Од тэмдгээс. **Wordmark ашиглаж болохгүй** — 32px дээр гараар бичсэн "shopping" уншигдахгүй |
| `og/site-card.jpg` | Үүдний холбоосын карт | Жинхэнэ wordmark-аас. **Гараар үүсгэдэг**, `build-og.py` үүнд хүрдэггүй |

### Логоны дэвсгэрийг тайрах арга
Wordmark-ыг картад тавихдаа зүгээр хуулбал логоны өөрийн `#f3f2ee` дэвсгэр
сайтын `#fbf7f0`-ээс ялгарч **дөрвөлжин ул мөр** үлдээнэ. Тиймээс гэрэлтүүлгээс
alpha бодож бэхийг нь салгана (`LO=60, HI=235`) — хатуу таславал гараар бичсэн
нарийн зураас тасархай болно.

### Цайвар дэвсгэрийн урхи
Хар загварт зориулсан бүх зүйл дахин шалгагдана:
- **Бүдэг бичвэр** — цагаан 50% нь хар дээр болдог ч, бэх 60% нь цөцгий дээр
  3.9:1 болж уншигдахаа болино. Одоо `--fg-dim` нь **0.72**, хамгийн бүдэг
  мөр 4.9:1
- **Логоны зураг** — саарал тэмдэг цөцгий дээр 2.9:1, бараг харагдахгүй
- **Hero-гийн хөшиг** — хар руу бүрхдэг байсныг `var(--bg)` болгосон
- **Маскийн градиент** — `#000`-ыг **хөндөхгүй**, тэнд өнгө биш alpha

---

## 14. `fb:app_id` анхааруулга — ШИЙДЭГДСЭН (2026-09-20)

✅ Өөрийн домэйн (`starshopping.mn`, §18) авч Meta-д баталгаажуулснаар арилсан: Sharing
Debugger шинэ картын хаяг дээр анхааруулгагүй, `fb:app_id` танигдаж байна. Доорх нь
`github.io` дээр яагаад засагддаггүй байсны түүх — домэйн алдагдвал буцаж ирнэ.

Facebook Sharing Debugger барааны карт дээр
`The following required properties are missing: fb:app_id` гэж **байнга**
заана. Шошго нь хуудсанд байгаа (`curl`-ээр батлагдсан). Энэ бол засагдахгүй.

**Шалтгаан:** `github.io` нь Public Suffix List дээр бүртгэлтэй — олон хүн
хуваалцдаг домэйн. Meta нь app-аа домэйнтэй холбохдоо тухайн домэйныг
**эзэмшихийг** шаарддаг. `starshopping-mn.github.io` бол GitHub-ын домэйн
доторх хэсэг тул App Domains-д нэмэх, баталгаажуулах боломжгүй.

**Юунд нөлөөлөхгүй:** холбоосын карт бүрэн ажиллана — зураг, нэр, үнэ
бүгд гарна. Батлагдсан.

### Туршиж үзээд БҮТЭЭГҮЙ зүйлс
- `fb:app_id` шошго нэмэх (2026-08-11) — шошго байгаа ч Meta хүлээж авдаггүй
- `og:type`-ыг `product` → `website` болгох (2026-08-11) — нүүр хуудас
  `website`-тайдаа өнгөрч байсан тул энэ нь шалтгаан гэж таамагласан.
  **Буруу таамаг байсан**, анхааруулга хэвээр. Өөрчлөлтийг үлдээсэн —
  redirect хуудсанд `website` илүү зөв, хор хөнөөлгүй.

### Хэзээ арилах вэ
Зөвхөн **өөрийн домэйн** (жишээ нь `starshopping.mn`) авч GitHub Pages-д
холбосон үед. Тэр үед Meta-гийн домэйн баталгаажуулалт ч ажиллана — энэ нь
iOS хэрэглэгчид рүү чиглэсэн хөрвөлтийн зарын оновчлолд шаардлагатай,
өөрөөр хэлбэл зөвхөн гоо сайхны асуудал биш.

### Facebook-ийн кэшийн дүрэм
Сайтад өөрчлөлт хийсний дараа **10 минут хүлээж байж** Scrape Again дар.
GitHub Pages `Cache-Control: max-age=600` илгээдэг тул эрт дарвал Facebook
хуучин хувилбарыг уншиж кэшилнэ. Нэг удаа яг ингэж төөрөгдсөн.

---

## 15. Telegram мэдэгдэл

⚠️ 2026-09-17-оос захиалга Apps Script-ээр дамжихаа больсон (§2) тул доорх нь
**зөвхөн хуучин замд** хамаарна. Шинэ захиалгын мэдэгдэл n8n талд байх ёстой.

Захиалга бүрд шууд Telegram-аар мэдэгдэнэ (`notifyTelegram_` — `Code.gs`).
Хуучин `notifyBatch` имэйл **10 захиалга тутамд** хэвээр ажиллана.

### ⚠️ Токеныг Code.gs дотор БИЧИХГҮЙ
`apps-script/Code.gs` нь **нийтийн GitHub repo**-д байдаг. Токен бичвэл хэн ч
уншиж, ботыг удирдана. Токен зөвхөн Apps Script-ийн **Script properties**
дотор амьдарна:

| Түлхүүр | Утга |
|---|---|
| `TELEGRAM_TOKEN` | BotFather-аас авсан токен |
| `TELEGRAM_CHAT` | Эзний chat id |

Claude токен авахгүй, кодод бичихгүй — эзэн өөрөө Apps Script дээрээ оруулна.

### Тавигдаагүй бол
`notifyTelegram_` чимээгүй буцна. Захиалга хэвийн бүртгэгдэнэ.

### Захиалгыг хэзээ ч унагаахгүй
Бүх дуудлага `try/catch` дотор, `muteHttpExceptions: true`. Telegram унасан ч
захиалга Sheet-д бичигдсэн хэвээр — мөнгө мэдэгдлээс чухал.

### Туслах функцууд
- `telegramTest()` — Run хийхэд туршилтын зурвас илгээж, Logger-т хариу бичнэ
- `telegramFindChatId()` — ботдоо зурвас бичсэний дараа Run хийвэл chat id-г олж өгнө

## 16. Хаягийн сонголт — захиалгын хуудас

Хаягийг бичүүлэхгүй, сонгуулна (2026-09-17). Бичсэн хаяг үйлчлүүлэгч → оператор →
хүргэлтийн компани гэж дамжихад дүүрэг, хороо буруу бичигдвэл оператор дахин
залгадаг байсан.

- **Өгөгдөл:** `data/mn-address.json` — 9 дүүрэг (хорооны тоо), 21 аймаг (330 сум).
  Гадны үйлчилгээ байхгүй, нэвтрэх шаардлагагүй. Хороо/сумын тоо өөрчлөгдвөл энэ
  файлыг л засна.
- **Ачаалалт:** барааны хуудас нээгдэхэд урьдчилан татна (`loadAddressData`).
  Захиалгын хуудас өгөгдлийг **дээд тал нь 2.5 секунд** хүлээгээд, ирээгүй бол
  хуучин бичих талбар (`#fAddr`) руу буцна. Хуудас хэзээ ч гацахгүй.
- **Хэлбэр:** Улаанбаатар / Орон нутаг → (дүүрэг → хороо) эсвэл (аймаг → сум,
  datalist — жагсаалтад байхгүй сумыг бичиж болно) → 3 мөр: (1) хороолол, байр /
  гудамж, (2) орц, давхар, тоот / хашааны дугаар, (3) тайлбар (заавал биш).
  Эхний 2 мөр заавал — байртай, тоотгүй хаяг оператороос дуудлага болдог.
  Байр ба гэр хороолол хоёр өөр хэлбэртэй тул placeholder-т хоёр жишээг хамт өгсөн.
  Backend руу нэг мөр явна: `Улаанбаатар, Хан-Уул дүүрэг, 15-р хороо, 3-р хороолол,
  45 байр, 2 орц, 5 давхар, 501 тоот · Тайлбар: код 1234`.
- **Хүргэлттэй уялдаа:** Орон нутаг сонговол хотын хүргэлтүүд түгжигдэж "Орон нутаг"
  (урьдчилж төлөх) автоматаар сонгогдоно, эсрэгээр ч мөн. `is-locked` — дарагдахгүй.
- **Санах:** сонголт `localStorage.ss_addr`-д хадгалагдана — дараагийн захиалгад бэлэн.
- Хэмжилт: `HANDOVER/measure/addr.js` (хуучин репо) — сонголт, орон нутаг, fallback,
  reload гурвуулаа тэнцсэн; нүүрний zoom ба бараа-линкийн хэмжилтэд өөрчлөлт алга.

## 17. Бараа нэмэх = зөвхөн маягт 13 (2026-09-18)

**Шинэ бараа, үнэ, зураг, нөөц — бүгд n8n маягт «13 · Бараа бүртгэх»
(`starshopping.app.n8n.cloud/form/product`).** Sheet-д бичихгүй, slug холбох
гүүр байхгүй, кодонд юу ч нэмэхгүй. Маягт `upsert_product` руу бичнэ, сайт
`web_products`-оос уншина, карт `tools/build-og.py`-аар 20 минутад үүснэ.

**Маягт 13-ын бүтэц (2026-09-18):** Form → «Маягтыг бэлтгэх» (Code: image_urls-ыг
мөрөөр задалж массив, featured → boolean, slug-ийн зайг зураас) → `upsert_product`
→ **«Supabase вэб талбар»** (PATCH `/rest/v1/products?product_id=eq.…`, body:
slug, description, image_urls, compare_at_mnt, featured) → «Хариу харуулах».
PATCH node байгаа шалтгаан: `upsert_product()` функц энэ 5 талбарыг **үл тоодог**
(хэмжсэн — `ok:true` буцаасан ч хүснэгтэд null үлдсэн), category-г л авдаг.
Функцийг засвал энэ node-ийг хасаж болно. Хариунд «Сайтын талбарууд: ✅/❌» мөр
PATCH-ийн HTTP кодыг хэлнэ — ❌ бол бараа бот дээр байгаа ч сайт дээр гарахгүй.

### Талбарууд ба дүрэм
| Талбар | Дүрэм |
|---|---|
| `product_id` | **Засах** бол буулга (маягтын хариунд байдаг), шинэ бол хоосон |
| `slug` | Латин үсэг, тоо, зураас: `Huwtsas-hadgalah-sags`. Сайтын хаяг `#/p/<slug>` ба картын хавтас. **Зар явуулсны дараа СОЛИХГҮЙ.** Хоосон бол сайт дээр **гарахгүй** |
| `category` | Sheet-ийн Categories-ийн slug: `ger-ahui`, `huuhdiin-heregsel`. Таарахгүй бол бараа тавиур дээр гарахгүй, шууд холбоосоор л нээгдэнэ |
| `description` | Эхний мөр гарчиг (≤30 тэмдэгт), дараа нь мөр тутамд нэг давуу тал; `Юу — тайлбар` бол «Юу» тодоор (`descBlock`) |
| `image_urls` | Мөр тус бүр нэг Drive **файлын** холбоос (`/file/d/…/view`), «Anyone with the link». Эхнийх = гол зураг. Хавтасны холбоос, файлын нэр ажиллахгүй (§3) |
| `price_mnt` | Зарах үнэ. Intake яг үүгээр тооцно |
| `compare_at_mnt` | Хямдралын өмнөх үнэ. `price_mnt`-ээс **их** бол зурсан хуучин үнэ + хувь харагдана; бага/хоосон бол юу ч гарахгүй |
| `stock_qty` | Хоосон = хязгааргүй. **0 = ДУУССАН**, товч хаагдана (§3-ын нөөцийн дүрэм). Sheet-ийн `Нөөц` энэ бараанд хамаагүй |
| `status` | `active` л сайт дээр гарна |
| `cost_mnt`, `why_this`, `max_per_order`, `low_stock_at`, `page_id`, `source_url`, `featured` | Эзний бот, тайлангийн талбарууд; сайт `featured`-ыг одоогоор ашигладаггүй |

### Шалгах
```bash
curl -s -X POST https://tdnjnqftxschbliumwwm.supabase.co/rest/v1/rpc/web_products \
  -H "apikey: $ANON" -H "Authorization: Bearer $ANON" -H "Content-Type: application/json" -d '{}'
```
(`$ANON` = `script.js` дэх `SUPABASE_ANON`.) Бараа slug, images, price_mnt-тэй
ирж байвал сайт дараагийн ачаалалтад харуулна. `python3 tools/health-check.py`
аль эх сурвалжаас уншсанаа хэлнэ («products from Supabase»).

### Нөөц ба нэг захиалгын дээд тоо (2026-09-20)
`web_products` `stock_qty` буцаадаг болмогц adapter түүнийг авна (үгүй бол
`in_stock`); 1–5 үлдсэн үед «Үлдсэн N ширхэг» өөрөө асна — **бодит тоо**, хэзээ ч
зохиомол ховордол биш. `max_per_order` → `p.maxPerOrder`; тоо ширхэгийн `+`
товч хоёр хуудсанд `orderCeiling(slug)` = min(үлдэгдэл, дээд тоо)-д зогсоно.
Intake хоёуланг шалгадаг — энэ нь зөвхөн хаягаа бичсэний дараа татгалзуулахгүйн тулд.

Sheet-ийн мөрийг slug-аар **сул тааруулж** (`loosen`) олдог: Supabase ба Sheet-ийн
slug том/жижиг үсгээр зөрсөн ч өнгө, хугацаа алдагдахгүй. ⚠️ Харин **картын
хавтас (`/p/<slug>/`) үсэг ялгадаг** — GitHub Pages дээр `/p/huuhdiin-…/` ба
`/p/Huuhdiin-…/` хоёр өөр хаяг (хэмжсэн: нэг нь 200, нөгөө нь 404). Backend
slug-ийн үсгийг өөрчилбөл явж буй зарын холбоос үхнэ.

### Түр хамаарал — мэдэж бай
- **Өнгө, хэмжээ, хүргэлтийн хугацаа** Supabase-д багана байхгүй тул Sheet-ийн
  Products дахь **ижил slug-тай мөрөөс** зээлнэ (`fromSupabase`, `...was`).
  Тэр мөрийг устгавал өнгөний сонголт, «5-7 хоногт» алга болно. Supabase-д
  багана нэмэгдвэл adapter-т нэг мөр нэмээд энэ хамаарлыг тасал.
- **n8n «02 · Захиалга бүртгэх»-ийн `SLUG_TO_ID`** — сайт Sheet-ээс явж байх
  үеийн гүүр. Сайт Supabase-аас уншиж эхэлмэгц (`web_products` бараатай) сайт
  UUID илгээнэ, map хэрэггүй; устгаад UUID биш ирвэл `product_not_found`
  буцаадаг хэвээр үлдээ.
- `web_products` хоосон бол сайт **Sheet-ээс** явна — алдаа биш, шилжилтийн
  байдал. Эрүүл мэндийн шалгалт үүнийг тэмдэглэнэ (fail биш).

## 18. Домэйн — starshopping.mn (2026-09-20)

Эзэн `starshopping.mn`-ийг авч GitHub Pages-д холбосон. Бүртгэгч: dns.mn
(нэрийн сервер `ns1–ns4.dns.mn`, TTL хамгийн багадаа **7200**).

| Бичлэг | Нэр | Утга |
|---|---|---|
| A ×4 | `@` | `185.199.108.153`, `.109.153`, `.110.153`, `.111.153` |
| CNAME | `www` | `starshopping-mn.github.io` |

- **`CNAME` файл repo-д БАЙХГҮЙ, хэрэггүй.** Pages нь GitHub Actions-аар deploy
  хийдэг тул домэйн repo-ийн **Settings → Pages → Custom domain**-д хадгалагдана.
  Файл нэмбэл юу ч өөрчлөгдөхгүй, зөвхөн төөрөгдүүлнэ.
- **Зарын холбоосын загвар: `https://starshopping.mn/p/<slug>/?ref=<creative_id>`** —
  `/`-тэй. `/`-гүй хувилбар ажиллана, гэхдээ нэг нэмэлт 301 хийдэг (хэмжсэн:
  hops=1, `?ref=` хадгалагдана).
- Хуучин `starshopping-mn.github.io/...` хаяг бүр **зам, `?ref=`-ээ хадгалаад**
  301-ээр шинэ домэйн рүү очно (хэмжсэн). Явж буй зарын холбоос, хуучин
  холбоосын картууд ажилласаар. `#`-ийн ардхыг хөтөч өөрөө авч үлдэнэ.
- Сайтын хаяг **дөрвөн газар**: `script.js` → `SITE_ORIGIN`, `tools/build-og.py`
  ба `tools/health-check.py` → `SITE`, `index.html` → canonical / og:url /
  og:image. Картууд (`p/`) `SITE`-ээс дахин үүснэ. Домэйн солигдвол энэ дөрөв.
- n8n маягт 13-ын хариун дахь «Сайт дээрх хаяг» мөн энэ домэйныг заана.
- `localStorage` origin-оор тусгаарлагддаг тул зочдын хадгалсан каталог, хаягийн
  сонголт (`ss_addr`), зарын мөр (`ss_ref`) шинэ домэйн дээр **шинээр эхэлнэ**.
  Нэг удаагийн зүйл; захиалга, өгөгдөлд нөлөөгүй.
- Supabase-ийн anon key, n8n webhook нь origin шалгадаггүй тул юу ч өөрчлөх
  шаардлагагүй байсан (шинэ домэйнээс хэмжиж баталсан).
- **Meta (2026-09-20-нд хийгдсэн):** домэйн **Starshopping** business portfolio-д
  (id `1023798047144762`) нэмэгдэж **Verified** болсон. Арга нь **meta-tag** —
  `index.html`-ийн `<head>` дэх `facebook-domain-verification` мөр. **Тэр мөрийг
  бүү хас**, статик `<head>`-д үлдээ (Meta JavaScript ажиллуулдаггүй). Домэйн нэг л
  portfolio-д харьяалагдана. Meta app `3475902549234725`-ийн App Domains =
  `starshopping.mn`, Privacy Policy URL = `/privacy.html`, Data deletion URL =
  `/privacy.html#data-deletion` — гурвуулаа шинэ домэйн дээр.
- Enforce HTTPS асаалттай: `http://` → `https://` 301, зам ба `?ref=` хадгалагдана.
- Домэйн сунгах хугацаа: **жил бүр** — дуусвал сайт, бүх зар, Meta-гийн баталгаажуулалт унана.
